apiVersion: v1
kind: Service
metadata:
  name: api-image-filter
  labels:
    app: api-feed
  namespace: udagram
spec:
  selector:
    app: api-image-filter
  ports:
   -  protocol: TCP
      port: 8080
      targetPort: 8080
