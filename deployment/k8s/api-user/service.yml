apiVersion: v1
kind: Service
metadata:
  name: api-user
  labels:
    app: api-feed
  namespace: udagram
spec:
  selector:
    app: api-user
  ports:
   -  protocol: TCP
      port: 8080
      targetPort: 8080
